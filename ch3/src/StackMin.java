public class StackMin {
    // note that this solution is not a stand-alone class.
    // do the following changes to the "Stack" class:
    // 1. add private stack "min"
    // 2. every time "push" is called, compare item to min.top: if it is null, directly push in,
    //    if item < or = min, push item in min stack
    // 3. every time "pop" is called, compare item to min.top: if they have the same value,
    //    pop the item out of the min stack
    // 4. to check min any time, peek at top of min stack
}
