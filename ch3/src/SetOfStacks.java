public class SetOfStacks {
    // add variable "limit", when it is reached, start new stack
    // put the "small" stacks (actually their pointers) in a big stack
    // when current stack is empty, pop next stack

    // for FOLLOW UP
    // have a counter of # of mini stacks
    // have a "temp" stack to push top stacks into and pop them back out after retrieval
    // not as space efficient as answer key
}
